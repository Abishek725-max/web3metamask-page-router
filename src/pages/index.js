"use client";
import Image from "next/image";
import {
  useWeb3AuthConnect,
  useWeb3Auth,
  useWeb3AuthDisconnect,
} from "@web3auth/modal/react";
import { useEffect } from "react";

export default function Home() {
  const { web3Auth, isConnected, provider, initError } = useWeb3Auth();
  const { connect, loading, isConnected: isConnecting } = useWeb3AuthConnect();
  const { disconnect } = useWeb3AuthDisconnect();

  const handleConnect = async () => {
    await connect(); // Opens Web3Auth modal for user login
  };

  return (
    <div className="font-sans grid grid-rows-[20px_1fr_20px] items-center justify-items-center min-h-screen p-8 pb-20 gap-16 sm:p-20">
      <main className="flex flex-col gap-[32px] row-start-2 items-center sm:items-start">
        <Image
          className="dark:invert"
          src="/next.svg"
          alt="Next.js logo"
          width={180}
          height={38}
          priority
        />
        <ol className="font-mono list-inside list-decimal text-sm/6 text-center sm:text-left">
          <li className="mb-2 tracking-[-.01em]">
            Create Next App Router
            <code className="bg-black/[.05] dark:bg-white/[.06] font-mono font-semibold px-1 py-0.5 rounded">
              src/app/page.js
            </code>
            .
          </li>
          <li className="tracking-[-.01em]">
            Save and see your changes instantly.
          </li>
        </ol>

        <div className="flex gap-4 items-center flex-col sm:flex-row">
          {!isConnected ? (
            <button
              className="rounded-full border border-solid border-black/[.08] dark:border-white/[.145] transition-colors flex items-center justify-center hover:bg-[#f2f2f2] dark:hover:bg-[#1a1a1a] hover:border-transparent font-medium text-sm sm:text-base h-10 sm:h-12 px-4 sm:px-5 w-full sm:w-auto md:w-[158px]"
              onClick={handleConnect}
              disabled={loading || isConnecting}
            >
              {loading || isConnecting ? "Connecting..." : "Connect Wallet"}
            </button>
          ) : (
            <>
              {" "}
              <button className="rounded-full border border-solid border-black/[.08] dark:border-white/[.145] transition-colors flex items-center justify-center hover:bg-[#f2f2f2] dark:hover:bg-[#1a1a1a] hover:border-transparent font-medium text-sm sm:text-base h-10 sm:h-12 px-4 sm:px-5 w-full sm:w-auto md:w-[158px]">
                Connected!
              </button>
              <button
                onClick={disconnect}
                className="cursor-pointer rounded-full border border-solid border-black/[.08] dark:border-white/[.145] transition-colors flex items-center justify-center hover:bg-[#f2f2f2] dark:hover:bg-[#1a1a1a] hover:border-transparent font-medium text-sm sm:text-base h-10 sm:h-12 px-4 sm:px-5 w-full sm:w-auto md:w-[158px]"
              >
                Logout
              </button>
            </>
          )}
        </div>
      </main>
    </div>
  );
}
